{
  "name": "@rivet/actions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "echo 'actions are composite YAML — no TypeScript to check'",
    "build": "echo 'composite actions call the CLI directly — nothing to bundle yet'"
  }
}
